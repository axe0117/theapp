import type { IAssetsUpdaterParams, TLanguage } from "../../types";
import type { DataManager } from "./DataManager";
import type { LocalizationManager } from "./LocalizationManager";
export declare class AssetsUpdater {
    languages: TLanguage[];
    private isFetching;
    private dataManager;
    private localizationManager;
    constructor({ languages, checkInterval, // 0.5 hour
    instant, force, }: IAssetsUpdaterParams, dataManager: DataManager, localizationManager: LocalizationManager);
    fetchAssets(force?: boolean): Promise<void>;
}
