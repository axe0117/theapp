import type { IConstellationData, TLanguage, TLocalizationData } from "../../types";
export declare class ConstellationFinder {
    private readonly data;
    private readonly localization;
    constructor(data: IConstellationData[], localization: TLocalizationData);
    getById(id: number): IConstellationData;
    getName(constellation: IConstellationData, language: TLanguage): string;
}
