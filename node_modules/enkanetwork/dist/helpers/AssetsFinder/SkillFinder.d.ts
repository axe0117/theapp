import type { ISkillData, TLanguage, TLocalizationData } from "../../types";
export declare class SkillFinder {
    private readonly data;
    private readonly localization;
    constructor(data: ISkillData[], localization: TLocalizationData);
    getById(id: number): ISkillData;
    getName(skill: ISkillData, language: TLanguage): string;
}
