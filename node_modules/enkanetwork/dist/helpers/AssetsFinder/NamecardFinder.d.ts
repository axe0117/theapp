import type { INamecardData, TLanguage, TLocalizationData } from "../../types";
export declare class NamecardFinder {
    private readonly data;
    private readonly localization;
    constructor(data: INamecardData[], localization: TLocalizationData);
    getById(id: number): INamecardData;
    getName(namecard: INamecardData, language: TLanguage): string;
}
