import type { TLanguage, TLocalizationData } from "../../types";
export declare class WeaponFinder {
    private readonly localization;
    constructor(localization: TLocalizationData);
    getName(nameTextMapHash: number, language: TLanguage): string;
}
