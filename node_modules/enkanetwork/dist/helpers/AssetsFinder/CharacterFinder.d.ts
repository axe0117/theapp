import type { ICharacterData, TLanguage, TLocalizationData } from "../../types";
export declare class CharacterFinder {
    private readonly data;
    private readonly localization;
    constructor(data: ICharacterData[], localization: TLocalizationData);
    getById(id: number, skillDepotId?: number): ICharacterData;
    getName(character: ICharacterData, language: TLanguage): string;
}
