import type { IProfilePictureData, TLanguage, TLocalizationData } from "../../types";
export declare class ProfilePictureFinder {
    private readonly data;
    private readonly localization;
    constructor(data: IProfilePictureData[], localization: TLocalizationData);
    getById(id: number): IProfilePictureData;
    getName(profilePicture: IProfilePictureData, language: TLanguage): string;
}
