import type { ICostumeData, TLanguage, TLocalizationData } from "../../types";
export declare class CostumeFinder {
    private readonly data;
    private readonly localization;
    constructor(data: ICostumeData[], localization: TLocalizationData);
    getById(id: number): ICostumeData;
    getName(costume: ICostumeData, language: TLanguage): string;
}
