import type { AssetsFinder } from "../../helpers";
import type { IShowAvatarInfoList, TLanguage } from "../../types";
export declare class CharacterPreview {
    id: number;
    costumeId: number | null;
    costumeName: string | null;
    name: string;
    icon?: string;
    level: number;
    constructor(assets: AssetsFinder, language: TLanguage, characterPreview: IShowAvatarInfoList);
}
