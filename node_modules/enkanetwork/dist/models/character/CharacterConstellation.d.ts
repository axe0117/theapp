import type { AssetsFinder } from "../../helpers";
import type { TLanguage } from "../../types";
export declare class CharacterConstellation {
    id: number;
    icon: string;
    name: string;
    unlocked: boolean;
    constructor(assets: AssetsFinder, language: TLanguage, talent: number, talents: number[]);
}
