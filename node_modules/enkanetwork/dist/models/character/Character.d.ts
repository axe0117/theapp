import type { AssetsFinder } from "../../helpers";
import type { IAvatarInfoList, TElement, TLanguage } from "../../types";
import { CharacterConstellation, CharacterReliquary, CharacterSkill, CharacterStats, CharacterWeapon } from "../index";
export declare class Character {
    id: number;
    name: string;
    icons: {
        avatar: string;
        side: string;
        gacha: string;
    };
    level: number;
    friendshipLevel: number;
    rarity: 5 | 4;
    element: TElement;
    elevations: number;
    xp: number;
    stats: CharacterStats;
    constellations: CharacterConstellation[];
    skills: CharacterSkill[];
    skillSetId: number;
    skillData: number[];
    weapon: CharacterWeapon;
    reliquaries: CharacterReliquary[];
    costumeId: number | null;
    costumeName: string | null;
    constructor(assets: AssetsFinder, language: TLanguage, characterInfo: IAvatarInfoList);
}
