import type { AssetsFinder } from "../../helpers";
import type { TLanguage } from "../../types";
export declare class CharacterSkill {
    id: number;
    icon: string;
    name: string;
    level: number;
    isBoosted: boolean;
    constructor(assets: AssetsFinder, language: TLanguage, skillId: number, level: number, proudSkillExtraLevelMap: Record<string, number>);
}
