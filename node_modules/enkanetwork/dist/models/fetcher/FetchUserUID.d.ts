import type { AssetsFinder } from "../../helpers";
import type { IProfileAvatarsInfo, TLanguage } from "../../types";
import { Character, PlayerInfo } from "../index";
export declare class FetchUserUID {
    player: PlayerInfo;
    characters: Character[];
    ttl: number;
    constructor(assets: AssetsFinder, language: TLanguage, data: IProfileAvatarsInfo);
}
